package com.lld.moviebooking.model;

import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;

@Getter
@Setter
@NoArgsConstructor
public class GuestUser  {
    public RegisteredUser register(String userName, String email, String password, String firstName, String lastName) {
        // RegisteredUser user = new RegisteredUser(userName, email, password, firstName, lastName);
        return null;
    }
}
